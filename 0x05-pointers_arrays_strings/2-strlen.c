#include <string.h>
/**
 * _strlen - A funtion that returns the length of a string
 *
 * @s: parameter for input
 *
 * Return: Always 0
 **/

int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
